@page "{id:int}"
@model BugTrackingSystem.Pages.Projects.EditModel
@{
    ViewData["Title"] = "Редагування проєкту";
}

<h1><i class="bi bi-pencil-square"></i> Редагування проєкту</h1>
<hr />

<div class="row">
    <div class="col-md-7">
        <div class="card shadow-sm">
            <div class="card-body">
                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                    <div class="form-group mb-3">
                        <label asp-for="Project.Name" class="control-label fw-bold">Назва проєкту</label>
                        <input asp-for="Project.Name" class="form-control" />
                        <span asp-validation-for="Project.Name" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-3">
                        <label asp-for="Project.Description" class="control-label fw-bold">Опис</label>
                        <textarea asp-for="Project.Description" class="form-control" rows="3"></textarea>
                        <span asp-validation-for="Project.Description" class="text-danger"></span>
                    </div>

                    <div class="form-group mb-4">
                        <label class="fw-bold mb-2">
                            <i class="bi bi-people"></i> Учасники проєкту
                        </label>
                        @if (!Model.AllUsers.Any())
                        {
                            <p class="text-muted small">Немає зареєстрованих користувачів.</p>
                        }
                        else
                        {
                            <div class="border rounded p-2" style="max-height:220px; overflow-y:auto;">
                                @foreach (var user in Model.AllUsers)
                                {
                                    <div class="form-check">
                                        <input class="form-check-input"
                                               type="checkbox"
                                               name="SelectedUserIds"
                                               value="@user.Id"
                                               id="user_@user.Id"
                                               @(Model.SelectedUserIds.Contains(user.Id) ? "checked" : "") />
                                        <label class="form-check-label" for="user_@user.Id">
                                            @user.UserName
                                        </label>
                                    </div>
                                }
                            </div>
                        }
                    </div>

                    <div class="form-group mt-2">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-lg"></i> Зберегти зміни
                        </button>
                        <a asp-page="Index" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left"></i> Скасувати
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
